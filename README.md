# evidencias
Repositorio de evidencias de ejercicios java y otros problemas.

Para crear un repositorio 
Crear en git hub New repository ...

Conectar el proyecto con git hub

git init
git add . 
git commit -m "commit"
git remote add origin (url de la repo)
-Chequear la rama main, si esta en master, cambiara  main
git branch 
si esta en master 
git checkout main
git push -u origin main
